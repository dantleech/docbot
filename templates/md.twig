{% block article -%}
{% if block.title -%}
{{ block.title }}
{% for i in range(1,block.title|length) -%}={% endfor %}


{% endif %}
{% for child in block.blocks %}
{{ render_block(child) }}
{% endfor %}
{% endblock %}

{% block assert_contains %}
{{- render_block(block.block) -}}
{% endblock %}

{% block create_file %}
```{{ block.language }}
{{ block.content }}
```
{% endblock %}

{% block shell %}
```shell
$ {{ block.content }}
{% if block.stdout and data.stdout|default(null) %}{{ data.stdout }}{% endif %}
{% if block.stderr and data.stderr|default(null) %}{{ data.stderr }}{% endif %}

```
{% endblock %}

{% block text %}
{{ block.text|sub_block_tokens(block.context) }}
{% if block.context %}{{ render_block(block.context) }}{% endif %}
{% endblock %}
{% block hidden %}
{% endblock %}

{% block section %}
{{ block.title }}
{% for i in range(1,block.title|length) -%}-{% endfor %}


{% for child in block.blocks %}
{{ render_block(child) }}
{%- endfor -%}
{% endblock %}

{% block show_file %}
``````{{ block.language }}
# {{ block.path }}
{{ data.contents }}
``````
{% endblock %}
